[package]
name = "vulcan-app"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[package.metadata.vulcan]
layer = "Application"

[dependencies]
vulcan-domain = { path = "../vulcan-domain" }
