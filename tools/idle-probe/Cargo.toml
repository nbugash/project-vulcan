[package]
name = "idle-probe"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = "0.2.2"
vulcan-adapters = { path = "../../crates/vulcan-adapters" }
vulcan-domain = { path = "../../crates/vulcan-domain" }

[package.metadata.vulcan]
layer = "Tooling"
